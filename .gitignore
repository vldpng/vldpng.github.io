node_modules/
build/
dist/
coverage/
tmp/

# Серверный бандл: лежит рядом с package.json, а не в dist/, потому что
# на проде dist/ становится document root и качается по HTTP.
server.cjs
server.cjs.map

.DS_Store
*.log
.env*
!.env.example

# База данных панели администратора (SQLite)
storage/

# Архивы для ручной заливки на хостинг: это та же сборка, что и dist/,
# упакованная зипом. Весят по 19 МБ и пересобираются одной командой.
*.zip
